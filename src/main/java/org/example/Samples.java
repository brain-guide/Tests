// это когда всплывающие окна самого сайта, что бы их закрыть, только если с одной кп=нопкой ОК
//Alert alert = browser.switchTo().alert();
//alert.accept();
//browser.switchTo().defaultContent();

//options.addArguments("headless"); // запуск  теста без открытия браузера
//ввод невалидного числа, например 3
//browser.findElement(By.xpath("//*[@name='zip_code']")).sendKeys("3");
//browser.findElement(By.xpath("//*[@value='Continue']")).click();
//String errorMsg = browser.findElement(By.cssSelector(".error_message")).getText();
//assertEquals(errorMsg, "Oops, error on page. ZIP code should have 5 digits");

// удаление введенных чисел
//browser.findElement(By.xpath("//*[@name='zip_code']")).sendKeys("1");
//browser.findElement(By.name("zip_code")).sendKeys(Keys.CONTROL + "A");
// browser.findElement(By.cssSelector("*[name='zip_code']")).sendKeys(Keys.BACK_SPACE);
